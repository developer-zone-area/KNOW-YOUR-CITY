# Production Environment Variables Template
# ⚠️ NEVER commit actual credentials to Git! This is a template only.
# Copy these variables to your deployment platform's environment variables settings.

# MongoDB Atlas Connection
# Get this from MongoDB Atlas → Clusters → Connect → Connect your application
# Replace YOUR_USERNAME, YOUR_PASSWORD, and YOUR_CLUSTER with actual values
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/know-your-city?retryWrites=true&w=majority&appName=knowYourCity

# JWT Secret (Generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Server Configuration
NODE_ENV=production
PORT=5000

# Client URL (Update with your deployed frontend URL)
CLIENT_URL=https://your-frontend-url.vercel.app

# Google OAuth (Optional - for social login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (Optional - for social login)
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
